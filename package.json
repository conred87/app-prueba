{
  "name": "amplify-static-site",
  "version": "1.0.0",
  "description": "Minimal static site for AWS Amplify",
  "scripts": {
    "build": "echo 'Nothing to build'",
    "test": "echo 'No tests'"
  }
}
