version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "No build step necessary"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
